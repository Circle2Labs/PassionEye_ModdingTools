namespace Code.Frameworks.Character.Enums
{
	public enum EBodyBlendShapeCategory
	{
		None,
		Chest, // breast size, depth, shape && nipple depth, size && areola size, depth
		Neck, // width, thickness
		Torso, // upper torso width, thickness && lower torso width, thickness && torso position, thickness
		Waist, // waist width, thickness
		Hips, // hips width, thickness && butt size, angle, spacing
		Arms, // shouder width, thickness && upper arm width, thickness && elbow width, thickness && forearm width, thickness && wrist scale
		Legs, // upper thigh width, thickness && lower thigh width, thickness && knee width, thickness && calves vertical position, definition && ankles width, thickness
		Privates // labia1 length, width, opening, spread && labia2 size, thickness, opening, spread && clitoris size && vaginal hole size
	}
}